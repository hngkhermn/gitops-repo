apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-db-server
spec:
  selector:
    app: db-server
  ports:
    - protocol: TCP
      port: {{ .Values.mysql.service.port }}
      targetPort: {{ .Values.mysql.service.port }}
